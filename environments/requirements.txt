# PID Flow Matching — dependencies
# PyTorch with CUDA 12.6 (NVIDIA drivers 560+, H100/A100)
#
# Check your CUDA version: nvidia-smi (top-right corner)
# For other CUDA versions see: https://pytorch.org/get-started/locally/

--index-url https://download.pytorch.org/whl/cu126
--extra-index-url https://pypi.org/simple

# PyTorch
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Core
numpy>=1.21.0
pillow>=9.0.0
PyYAML>=6.0
einops>=0.6.0
tqdm>=4.64.0
scipy>=1.9.0

# Evaluation metrics (FID, KID)
torch-fidelity>=0.3.0
pandas>=1.3.0
matplotlib>=3.4.0

# Experiment tracking
wandb>=0.13.0

# Dataset loading from HuggingFace Hub
datasets>=2.0.0
huggingface-hub>=0.16.0
